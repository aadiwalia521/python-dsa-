add 0 to the last 
arr = [0, 1, 0, 3, 12]

result = []
zero_count = 0

for x in arr:
    if x != 0:
        result.append(x)
    else:
        zero_count += 1

result.extend([0] * zero_count)
print(result)

rev of arr 
arr = [1, 2, 3, 4]
rev = []

for i in range(len(arr) - 1, -1, -1):
    rev.append(arr[i])

print(rev)

